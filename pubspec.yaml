name: music_playlist_app
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: 3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.0.2
  get: ^4.6.5
  dio: ^5.1.2
  cached_network_image: ^3.2.3
  json_annotation: ^4.8.1
  freezed_annotation: ^2.2.0
  get_it: ^7.6.0
  flutter_screenutil: ^5.8.1
  intl: ^0.18.1
  shimmer: ^2.0.0
  flutter_animate: ^4.1.1+1 # not use
  audioplayers: ^4.0.1
  font_awesome_flutter: ^10.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.2
  freezed: ^2.3.4

flutter:
  uses-material-design: true
  assets:
    - .env.development
    - .env.staging
    - .env.production
    - assets/icons/
